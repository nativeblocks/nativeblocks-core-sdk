# nativeblocks-core-sdk

The Nativeblocks: a runtime for Android and iOS host libraries — `runtime`, `compiler`, `foundation`, `devkit`.

Docs, guides and integration steps: **[nativeblocks.io](https://nativeblocks.io)**
